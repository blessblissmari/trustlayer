# TrustLayer — frontend (Cloudflare Pages) environment
# Copy to frontend/.env.local for local dev. In Pages, set these in the
# project's Environment Variables UI (both Production and Preview).

# Backend API base URL (Yandex API Gateway)
VITE_API_BASE=https://d5dtrq5lq1kjvv45ufkb.6brbn2wz.apigw.yandexcloud.net

# Supabase (public anon key only — service_role MUST NOT be exposed to the browser)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
